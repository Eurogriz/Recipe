apiVersion: apps/v1
kind: Deployment
metadata:
  name: formulation-workbench
spec:
  template:
    spec:
      containers:
        - name: api
          resources:
            requests:
              cpu: "250m"
              memory: "512Mi"
            limits:
              cpu: "2"
              memory: "1Gi"
