"""Domain layer.

Pure Python entities, value objects, domain services and domain events.
No external dependencies (except stdlib).
This is the heart of Clean Architecture — all other layers depend on this.
"""
